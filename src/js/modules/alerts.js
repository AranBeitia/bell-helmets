const invalidLength = `
  <p class="warning"><strong>Holy guacamole!</strong> Incorrect length of value</p>
`

const invalidType = `
  <p class="warning"><strong>Holy guacamole!</strong> Incorrect type of value</p>
`

const invalidPassword = `
  <p class="warning"><strong>Hello kitty!</strong> Password must be (>8, n, N, 123..., *@%...)</p>
`

const samePassword = `
  <p class="warning"><strong>Hello kitty!</strong> Password is not the same</p>
`
const success = `
  <p class="warning"><strong>Hello ween!</strong> The field must be length between 5 and 20 </p>
`

export { invalidLength, invalidType, invalidPassword, samePassword }

